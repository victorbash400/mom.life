import { useFamily } from "./FamilyProvider";
import Image from "next/image";
import styles from "./AppHeader.module.css";
export function AppHeader({ pluginsOpen, onChildrenOpen, onProfileOpen, onPluginsToggle }: { pluginsOpen: boolean; onChildrenOpen: () => void; onProfileOpen: () => void; onPluginsToggle: () => void }) { const { family: { parent } } = useFamily(); return <header className={styles.header}><button aria-label={`Open ${parent.name}'s profile`} className={styles.greeting} onClick={onProfileOpen} type="button"><Image src="/sarah-profile.png" alt={parent.name} width={44} height={44} priority /><span>Hi, {parent.name}</span></button><nav className={styles.actions} aria-label="Family controls"><button aria-label="Child Management" onClick={onChildrenOpen} type="button"><Image className={styles.assetIcon} src="/kid-svgrepo-com.svg" alt="" width={38} height={38} /></button><button aria-expanded={pluginsOpen} onClick={onPluginsToggle} type="button" aria-label="Connections"><Image className={styles.assetIcon} src="/plugin-svgrepo-com.svg" alt="" width={35} height={35} /></button><button aria-label="Safety" title="Safety" type="button"><Image className={styles.assetIcon} src="/safety-certificate-svgrepo-com.svg" alt="" width={36} height={36} /></button></nav></header>; }
