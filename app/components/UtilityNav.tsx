import { toolDirectory } from "../data/toolDirectory";
import styles from "./UtilityNav.module.css";
export function UtilityNav({ connectedIds }: { connectedIds: string[] }) { const connected = toolDirectory.filter((tool) => connectedIds.includes(tool.id)); if (!connected.length) return null; return <nav className={styles.nav} aria-label="Connected tools">{connected.map((tool) => { const Icon = tool.icon; return <button aria-label={tool.name} key={tool.id} style={{ "--tool-color": tool.color } as React.CSSProperties} title={tool.name} type="button"><Icon aria-hidden="true" /></button>; })}</nav>; }
